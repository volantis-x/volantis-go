export type Config = {
  FAVICON: string;
  DEFAULT_LOCALE: string;
  TITLE: string;
  DESCRIPTION: string;
  VIEW_TRANSITIONS: boolean;
  AUTHOR: string;
};

export type Metadata = {
  TITLE: string;
  DESCRIPTION: string;
};
