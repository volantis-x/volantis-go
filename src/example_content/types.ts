export type Config = {
  FAVICON: string;
  DEFAULT_LOCALE: string;
  TITLE: string;
  DESCRIPTION: string;
  AUTHOR: string;
  ADD_TITLE: boolean;
  DELIMITER: boolean;
  VIEW_TRANSITIONS: boolean;
};

export type Metadata = {
  TITLE: string;
  DESCRIPTION: string;
};
