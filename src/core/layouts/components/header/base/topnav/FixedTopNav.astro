---
// base 主题当前不支持 "fixed" 行为，选择此选项将加载 "sticky" 导航样式。

// 即使主题本身不支持 "fixed" 行为，也请保留此文件，以便 `normalizeBehavior()` 函数能够正确检测并加载各个主题的导航样式。

// The "base" theme currently does not support "fixed" behavior.
// Selecting this option will load the "sticky" navigation style instead.

// Even if the theme itself doesn't support "fixed" behavior,
// please keep this file. This allows the `normalizeBehavior()` function
// to correctly detect and load navigation styles for different themes.
---
