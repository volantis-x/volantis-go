---
// base 主题当前不支持 "one" 单列的头部导航，选择此选项将加载 "two" 双列导航样式。

// 即使主题本身头部导航不支持 "one" 单列的头部导航，也请保留此文件，以便能正确加载。

// base theme currently does not support the "one" single-column header navigation.
// Selecting this option will load the "two" double-column navigation style instead.

// Even if the theme itself does not support the "one" single-column header navigation,
// please keep this file to ensure that the navigation can be loaded correctly.
---
