---
import { THEME } from "@THEME";
import { TOP_MARQUEE } from "@/content/header.config";
import BaseTopMarquee from "@/components/header/base/TopMarquee.astro";
---

<header>
  {
    TOP_MARQUEE?.ENABLE && (
      <>
        {{
          base: <BaseTopMarquee />,
        }[THEME] || <BaseTopMarquee />}
      </>
    )
  }
</header>
